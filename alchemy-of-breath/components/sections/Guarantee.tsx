// Removed — the original Transforming Anxiety landing page does not
// include a money-back guarantee, so we don't fabricate one here.
// File kept as a stub so existing imports don't break during transition.
export function Guarantee() {
  return null;
}
